
// * map - percorre o array e retorna um novo array com a mesma quantidade de elementos
// * usado para transformar os elementos de um array
// * no caso do exemplo aplicar o dobro do valor

let numeros = [1, 2, 3, 4, 5];

let dobro = numeros.map((numero) => numero * 2);
console.log(dobro);
